apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deploy
  namespace: demo-k8s   # 👈 ให้ใช้ demo-k8s ชัดเจน
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx-demo
  template:
    metadata:
      labels:
        app: nginx-demo
    spec:
      containers:
      - name: nginx
        image: localhost:5001/nginx-custom:3.0       # 👈 เปลี่ยนเป็น 2.0
        imagePullPolicy: IfNotPresent  # 👈 ให้ใช้ image local ก่อน
        ports:
          - containerPort: 80


---
apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  namespace: demo-k8s   # 👈 ใช้ namespace เดียวกัน
spec:
  type: NodePort
  selector:
    app: nginx-demo
  ports:
    - port: 80          # Service Port
      targetPort: 80    # Container Port
      nodePort: 30081   # NodePort (จะใช้เลขอื่นในช่วง 30000–32767 ก็ได้)
