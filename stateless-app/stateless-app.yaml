apiVersion: apps/v1
kind: Deployment
metadata:
  name: stateless-app-deploy
  namespace: app-demo
  labels:
    app: stateless-app
spec:
  replicas: 3                        # Stateless → scale หลาย replica ได้สบาย
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: stateless-app
  template:
    metadata:
      labels:
        app: stateless-app
    spec:
      containers:
        - name: stateless-app
          image: your-docker-username/stateless-app:1.0   # 👈 แก้เป็น image ของคุณ
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000       # port ใน container
          resources:                    # ตัวอย่าง resource (ปรับได้)
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
          readinessProbe:               # เช็คว่าพร้อมรับ traffic หรือยัง
            httpGet:
              path: /hello
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:                # เช็คว่าตาย/แฮงก์ไหม
            httpGet:
              path: /hello
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10

---
apiVersion: v1
kind: Service
metadata:
  name: stateless-app-svc
  namespace: app-demo
  labels:
    app: stateless-app
spec:
  type: ClusterIP                  # ใช้ภายใน cluster
  selector:
    app: stateless-app
  ports:
    - name: http
      port: 80                    # port ที่ service เปิดให้คนอื่นเรียก
      targetPort: 3000            # ยิงเข้า containerPort 3000
